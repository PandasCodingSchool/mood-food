# NOTE: fastapi/uvicorn/pydantic bumped to satisfy the mcp SDK, which requires
# starlette>=1.x (incompatible with the previous fastapi==0.115.0 pin).
fastapi==0.138.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.5.2
langchain==0.3.7
langchain-openai==0.2.9
httpx==0.27.2

# Swiggy MCP (Food server) client — streamable HTTP transport
mcp==1.28.1

# testing
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.14.0
httpx==0.27.2
