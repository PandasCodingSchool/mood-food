# Local development backend URL
VITE_API_URL=http://localhost:3001/api

# Enable live Swiggy discovery (Phase 1). Off by default so the app degrades to
# the existing deep-link behaviour when the Swiggy integration isn't configured.
VITE_SWIGGY_LIVE=true
